<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:nestedScrollingEnabled="false">
    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:background="@drawable/background"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context="palarax.e_key_card.MainActivity"
        tools:showIn="@layout/app_bar_main"
        android:nestedScrollingEnabled="false">

        <Button
            android:id="@+id/vcard"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/vcard"
            android:textSize="12sp"
            android:drawablePadding="10dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/plainMessage"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/plainMessage"
            android:textSize="12sp"
            android:layout_below="@id/vcard"
            android:layout_marginBottom="10dp"
            android:drawablePadding="10dp" />

        <Button
            android:id="@+id/teleNumber"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/teleNumber"
            android:layout_below="@id/plainMessage"
            android:textSize="12sp"
            android:layout_marginBottom="10dp"
            android:drawablePadding="10dp" />

        <Button
            android:id="@+id/link"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/link"
            android:textSize="12sp"
            android:drawablePadding="10dp"
            android:layout_marginBottom="10dp"
            android:layout_below="@id/teleNumber"/>


        <Button
            android:id="@+id/overwriteNFCbtn"
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/tag_overwrite"
            android:textSize="12sp"
            android:layout_alignParentBottom="true"
            android:drawablePadding="10dp"
            />

        <Button
            android:id="@+id/writeRecordBtn"
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/tag_record_write"
            android:textSize="12sp"
            android:layout_centerInParent="true"
            android:layout_alignParentBottom="true"
            android:drawablePadding="10dp"
            />

        <Button
            android:id="@+id/clearBtn"
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:background="@drawable/read_barcode_btn"
            android:text="@string/tag_clear"
            android:textSize="12sp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:drawablePadding="10dp"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:layout_centerHorizontal="true"
            android:id="@+id/test"
            android:layout_below="@+id/link"/>

    </RelativeLayout>
</FrameLayout>